# Touhou Lab — Gallery

Generated images from CPU-only SD 1.5 + per-character LoRA training on an
Oracle Cloud ARM instance. Populated automatically by `commit_daily.sh`.

Each image has a sidecar `<image>.json` describing the prompt, seed, steps,
and LoRA checkpoint used.
